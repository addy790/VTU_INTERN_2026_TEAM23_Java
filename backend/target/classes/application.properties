# Database Configuration
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://aws-1-ap-northeast-1.pooler.supabase.com:6543/postgres?prepareThreshold=0}
spring.datasource.username=${DATABASE_USERNAME:postgres.yqykxjkpetxfapkxjgtt}
spring.datasource.password=${DATABASE_PASSWORD:6,DQr9euY&qiviF}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT Configuration
pat.app.jwtSecret=${JWT_SECRET:PLACEMENT_COMPASS_VERY_SECRET_KEY_FOR_LOCAL_DEMO_SHOULD_BE_LONG_ENOUGH_FOR_HS256}
pat.app.jwtExpirationMs=86400000

# OTP Configuration
pat.app.otpExpirationMs=300000

# Server Configuration
server.port=${PORT:8080}

# Email Configuration (SMTP)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:adithihebbar70@gmail.com}
spring.mail.password=${MAIL_PASSWORD:hlqd wopn nwqf zfuc}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
